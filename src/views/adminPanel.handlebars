<main>
    <header>
        <h1>¡Bienvenido Amin!</h1>
        <p>Aquí puedes administrar los usuarios.</p>
    </header>

    <article>
        {{#if usersInfo}}
            {{#each usersInfo}}
            <div class="contenedorUsuarios">
                <ul>
                    <li>Name: {{this.first_name}} {{this.last_name}}</li>
                    <li>Email: {{this.email}}</li>
                    <li>Age: {{this.age}}</li>
                    <li>Role: {{this.role}}</li>
                    <li>Last connection: {{this.last_connection}}</li>
                </ul>
                <button class="changeRole" value={{this.email}}>Change role</button>
                <button class="deleteUser" value={{this.email}}>Delete user</button>
            </div>
            {{/each}}
        {{else}}
            <h3>¡No hay usuarios registrados!</h3>
        {{/if}}
        
    </article>


</main>

<script src="js/adminPanel.js"></script>